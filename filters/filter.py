from PIL import Image, ImageFilter
import numpy as np
import os
import cv2 as cv

# INPUT_DIR = "assets"
# OUTPUT_DIR = "assets_output"
# image = Image.open(os.path.join(INPUT_DIR,'hector2.jpeg'))
# filtered = image.filter(ImageFilter.FIND_EDGES)
# filtered.show()